<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}SOPHIAX{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>💠</text></svg>">
        
        <!-- Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div class="app-container">
            <!-- Sidebar -->
    <aside class="app-sidebar">
                <div class="brand-logo">
                    <span>💥</span> SOPHIAX
                </div>
                
                <nav class="nav-links">
                    <a href="/" class="nav-item active">
                        Dashboard
                    </a>
                    
                    <a href="{{ path('app_sops_index') }}" class="nav-item">
                        All SOPs
                    </a>
                    
                    <a href="#" class="nav-item">
                        Learning Paths
                    </a>
                    
                    <a href="#" class="nav-item">
                        Categories
                    </a>
                    
                    <a href="{{ path('app_user') }}" class="nav-item">
                        Users
                    </a>
                </nav>
                
                <div class="user-profile-mini">
                    <div class="avatar">PO</div>
                    <div style="font-size: 0.875rem;">
                        <div style="font-weight: 600;">Purple Orca</div>
                        <div style="color: #64748b; font-size: 0.75rem;">Admin</div>
                    </div>
                </div>
            </aside>

            <!-- Main Content -->
            <main class="main-content">
                <header class="app-header">
                    <div class="search-bar">
                    <p>page header</p>
                        </div>
                    
                    <div class="header-actions">
                        <button class="btn-icon">
                            <p>Notification</p>
                            </button>
                    </div>
                </header>

                <div class="page-container">
                    {% block body %}{% endblock %}
                </div>
            </main>
        </div>
    </body>
</html>
