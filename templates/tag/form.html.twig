{% extends 'base.html.twig' %}

{% block title %}{{ tag ? 'Edit' : 'Add' }} Tag - SOPHIAX{% endblock %}
{% block page_title %}{{ tag ? 'Edit Tag' : 'Add New Tag' }}{% endblock %}

{% block body %}
<div class="max-w-md">
    {# Breadcrumb #}
    <nav class="flex items-center gap-2 text-sm mb-6">
        <a href="{{ path('app_tags_index') }}" class="text-slate-500 hover:text-indigo-600">Tags</a>
        <iconify-icon icon="solar:alt-arrow-right-bold" class="text-slate-400" width="16" height="16"></iconify-icon>
        <span class="text-slate-800 font-medium">{{ tag ? 'Edit' : 'Add' }}</span>
    </nav>

    {# Form Card #}
    <div class="bg-white rounded-xl shadow-sm border border-slate-200 p-6">
        <form method="post" class="space-y-6">
            {# Name #}
            <div>
                <label for="name" class="block text-sm font-medium text-slate-700 mb-2">
                    Tag Name <span class="text-red-500">*</span>
                </label>
                <input 
                    type="text" 
                    name="name" 
                    id="name" 
                    value="{{ tag ? tag.name : '' }}"
                    class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:ring-2 focus:ring-purple-500 focus:border-purple-500 transition-all outline-none"
                    placeholder="e.g., critical, beginner, 2024"
                    required
                >
            </div>

            {# Actions #}
            <div class="flex items-center justify-between pt-4 border-t border-slate-200">
                <a href="{{ path('app_tags_index') }}" class="text-slate-600 hover:text-slate-800 font-medium">
                    Cancel
                </a>
                <button 
                    type="submit" 
                    class="bg-purple-600 hover:bg-purple-700 text-white font-medium py-2.5 px-6 rounded-lg transition-colors"
                >
                    {{ tag ? 'Update' : 'Create' }} Tag
                </button>
            </div>
        </form>
    </div>
</div>
{% endblock %}
